How to Run
1. Start up Docker (or Docker Desktop
2. Run the following command in your terminal to start docker containers:
```docker compose up -d```
3. kafka-ui will be on http://localhost:8080/
4. postgres admin ui will be on http://localhost:80/ server name mydatabase
5. start WikimediaStreamProducerApplication
6. start WikimediaStreamConsumerApplication